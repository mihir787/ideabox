<h1>Category Listing</h1>

<%= flash[:message] %>
<ul>
  <%= @categories.each do |category| %>
    <li><%= category.name %> | <%= link_to "Update", edit_admin_category_path(category) %> | <%= link_to "Delete", admin_category_path(category), method: "delete" %></li>
  <% end %>
</ul>
